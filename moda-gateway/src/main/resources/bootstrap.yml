spring:
  application:
    name: moda-gateway
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: localhost:8848
        shared-dataids: moda-common-base.yaml
        refreshable-dataids: moda-common-base.yaml
