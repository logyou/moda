spring:
  profiles:
    active: dev
  application:
    name: moda-demo-service
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: 127.0.0.1:8848
        shared-dataids: moda-common-base.yaml,moda-common-service.yaml
        refreshable-dataids: moda-common-base.yaml,moda-common-service.yaml